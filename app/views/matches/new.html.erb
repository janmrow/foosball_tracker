<%= form_for @match do |f| %>
  <%= f.collection_select :winner_player_id, Player.all, :id, :firstname %>
  <%= f.collection_select :loser_player_id, Player.all, :id, :firstname %>
  <%= f.select :loserscore, options_for_select([["1"], ["2"]]) %>
  <%= f.label :date %>
  <%= f.submit %>
<% end %>