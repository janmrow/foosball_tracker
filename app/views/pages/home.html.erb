      <div class="row">
        <div class="col-md-12">
          <h1>Hello!</h1><p class="lead">Welcam to Foosbal Tracker. What do you want to do?</p>
          <div class="row">
            <div class="col-md-9 col-md-push-3"><%= link_to "Add player", new_player_path, class: "btn btn-default" %></div>
            <div class="col-md-3 col-md-pull-9"><%= link_to "Add match", new_match_path, class: "btn btn-default" %></div>
            <div class="col-md-3 col-sm-6 post"></div>
        </div>
      </div>
      </div>
      <div class="row">
        <div class="col-md-12">
          <h2>Currently 4 best players</h2>
        </div>
          <% if @top_players %>
            <% @top_players.each do |top_player| %>
            <div class="col-md-3 col-sm-6 post">
              <%= link_to image_tag(top_player.avatar.url(:medium)), player_path(top_player), class: "thumbnail" %>
              <h2><%= top_player.firstname + " " + top_player.lastname %></h2>
              <p>Also called @<%= top_player.nickname %>.</p>
            </div>
            <% end %>
          <% else %>
            <div class="col-md-12">
              <p>There are no top players yet! Reason is simple: there aren't at least four players who have played their first match.</p>
            </div>            
          <% end %>
        </div>    
      <div class="row">
        <div class="col-md-12">
          <h2>See the whole ranking</h2>
        </div>
          <div class="col-md-3 col-sm-6 post">
          <%= link_to "Go to rank", players_path, class: "btn btn-default" %>
        </div>          
      </div>